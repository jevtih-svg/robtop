-- 030: удаление плитки «Задания» из реестра — модуль слит в «Копилку» (заказ Джеффа 2026-06-09).
-- UI заданий теперь живёт во вкладке «Задания» модуля bank; отдельный модуль tasks удалён.
-- ВАЖНО (аддитивность + §10 ГАЙД-задания.md): таблицу tasks и сервис api/tasks.php НЕ трогаем —
-- сервис заданий не гейтится плиткой и продолжает работать. Убираем ТОЛЬКО строку реестра,
-- чтобы плитка исчезла с главного экрана и из «Управления приложениями».
-- Идемпотентно: повторный прогон просто не находит строку (0 затронутых), не падает.

DELETE FROM modules WHERE id='tasks';
