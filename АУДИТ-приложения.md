# Аудит приложения RobTop — что есть, чего нет

Дата: 2026-06-07, версия приложения на момент аудита: `v2026.06.07.3`.
Метод: сплошной проход по коду (`app/index.html`, `app/core/*`, `app/modules/*`, `app/api/*`, `app/family.html`), сверка с прототипами (`family-prototype.html`, `app-prototype.html`, `email-prototypes.html`) и планом `ПЛАН-аккаунты-и-приглашения.md`.
Обновлять этот файл при каждом изменении состава функций.

---

## 1. Поверхности интерфейса — что ЕСТЬ сейчас

### 1.1 Lock-экран (вход в приложение, `index.html` + `shell.js`)
- Форма входа: email (родитель) или никнейм (ребёнок) + пароль.
- Вход по одноразовому 1234 → обязательная смена пароля прямо здесь.
- Кнопка «Я новый родитель — создать семью» → регистрация (никнейм, email, пароль) → вход.
- «Забыли пароль?» (с v2026.06.07.14): форма email → op `forgot` → письмо со ссылкой сброса (только родители; детям пароль сбрасывает родитель).
- До подтверждения сессии плитки не рендерятся и реестр модулей не запрашивается.
- Демо-режим (открытие file://) работает без входа, с бейджем demo.

### 1.2 Главный экран
- РЕБЁНОК и демо: активные плитки всегда сверху, ниже за разделителем «Скоро будут» — плитки «скоро»; кнопка ⚙, индикатор версии слева внизу (`#appver`), фон-анимация. HUD-счётчики «приложений/доступно» с v2026.06.07.10 убраны — HUD виден только внутри модулей.
- РОДИТЕЛЬ (с v2026.06.07.6): вместо плиток — **родительский дашборд** (`core/parent.js`, секция `#parent`), read-only: вкладки Обзор (период 7/30, HUD событий/активных дней/очков, график по дням, разбивка и сводка по приложениям, заметки-инсайты) / Виш-лист ребёнка (разделы, деталь с историей, фото по правилу приватности) / Журнал (фильтры по модулям, шум `opened_*`/`viewed_*` и `bank` скрыт). Свой таббар внизу (глобальный HUD скрыт), ⚙ и переключение детей в топбаре. Возврат из любого модуля (`showHome`) ведёт родителя на дашборд. Данные: `api/parent.php`.

### 1.3 Настройки (⚙), по ролям
С v2026.06.07.10 настройки — **отдельный экран** (`#settings`, не шторка): «назад» слева вверху (возврат: ребёнок → плитки, родитель → дашборд), язык — сегмент-контрол в одну строку, «Выйти» — одна кнопка внизу экрана.
| Секция | Родитель | Ребёнок | Демо |
|---|---|---|---|
| Аккаунт (кто вошёл / форма входа); «Выйти» вынесен вниз экрана | ✅ | ✅ | подсказка |
| Семья — ПОЛНЫЙ центр (с v2026.06.07.14, слияние с консолью family.html): родители семьи (владелец/родитель, «вы»), дети с пилюлей «гость» у provisional, по тапу — сброс пароля → 1234 / «Передать родителю» (transfer_child: email → ссылка) / блокировка; ＋ добавить ребёнка; ✉ пригласить второго родителя; список отправленных приглашений (ждёт/принято/отозвано/истекло, ↻ новая ссылка, ✕ отозвать) | ✅ | — | — |
| Друзья: «Позвать друга» — ссылка-приглашение для друга (без email) | — | ✅ | — |
| Язык EN/RU/LV | ✅ | ✅ | ✅ |
| Управление приложениями | ✅ (родительская сессия) | — | ✅ (демо открыто; PIN упразднён) |

### 1.4 Управление приложениями («магазин», админка приложения)
- Список всех модулей: вкл/выкл, порядок (▲▼), удаление установленных, установка из `.json`-бандла.
- Гейт `rt_admin_gate`: ТОЛЬКО активная родительская сессия; в демо (file://) магазин открыт как песочница. PIN-форма и PIN-fallback удалены полностью (2026-06-07, v2026.06.07.13).
- Отдельной «десктопной админки» нет и не планировалась: это «Управление приложениями» в настройках (шторка магазина открывается поверх экрана настроек), на десктопе и мобильном одинаково.

### 1.4б admin.html (мастер-админка, с 2026-06-07)
- Отдельная консоль `app/admin.html` + `api/admin.php`, гейт `rt_require_admin` (родитель + флаг в `admins`, миграция 008). Спецификация: `ПЛАН-мастер-админка.md`.
- Разделы: дашборд-статистика, пользователи (переименовать/сброс→1234/блок/удалить/права админа/очки), семьи (привязать-отвязать родителя, перенос ребёнка, полный БАН с запретом повторной регистрации по email-хэшу), приглашения, контент модулей (generic-CRUD, картинки только фактом), очки (балансы+транзакции), письма (триггеры+произвольное), приложения (вкл/выкл/порядок глобально), журнал, админы и баны.
- Первый админ — одноразовый SQL (см. план §1), дальше гранты из панели.

### 1.5 family.html (ТОЛЬКО посадочные ссылок из писем; консоль упразднена v2026.06.07.14)
- Осталось ровно две посадочные: `family.html?invite=<код>` (принятие приглашений всех типов, для child_to_child — со сменой одноразового 1234) и `family.html?reset=<код>` (новый пароль). После успеха — переход в приложение.
- Консоль (вход/регистрация/дашборд семьи/история приглашений) УДАЛЕНА — всё это живёт в приложении: lock-экран (вход, регистрация, «Забыли пароль?») и настройки → Семья. Открытие family.html без параметров сразу уводит в приложение (`location.replace('index.html')`).
- Наружу уходят КОРОТКИЕ ссылки (v2026.06.07.5): `…/robtop/i/<КОД>` (приглашение, код 6 знаков) и `…/robtop/r/<КОД>` (сброс, 8 знаков) — корневой `.htaccess` 302-редиректит их на посадочные. Алфавит без похожих символов (0/O/1/I/L нет), регистр на входе нормализуется, в БД только sha256-хэш кода, старые длинные токены тоже принимаются.

### 1.6 Модули (есть в интерфейсе)
| Модуль | Статус | Что умеет |
|---|---|---|
| Виш-лист | active | желания, фото (загрузка на сервер), статусы, статистика |
| Чистка зубов | active | таймер, музыка во время чистки (27 треков, следующий/случайный/пропуск), родительская панель: вкладки История и Музыка (родителю — без PIN) |
| Слова наоборот | active | игра, история |
| Оценка дня | active | оценка звёздами после 20:00, фото дня, «почему/что понравилось», одна запись в день |
| words/ | ❌ пустая локальная папка | файлов нет, в реестре нет, в git не попадёт (мусор или чей-то задел) |
| Плитки «скоро» | soon в реестре | настроение, угадай число, смешные имена, счётчик дней, найти предмет, домашний музей, бюро находок, копилка, магазин |

---

## 2. Карта API — операция → есть ли UI

### 2.1 `api/accounts.php` (16 операций)
| Операция | UI в приложении | UI в family.html |
|---|---|---|
| register_parent | ✅ lock-экран | — (консоль удалена) |
| login / logout / me | ✅ | — |
| set_password (форс-смена 1234) | ✅ | ✅ (после accept ребёнка) |
| forgot (письмо со ссылкой) | ✅ «Забыли пароль?» на lock-экране (v2026.06.07.14) | — |
| reset (по токену) | — (ссылка из письма ведёт на посадочную — так и задумано) | ✅ посадочная |
| add_child / reset_child / set_child_status | ✅ настройки → Семья | — |
| invite co_parent | ✅ настройки → Семья | — |
| invite child_to_child | ✅ настройки → Друзья (ребёнок) | — |
| invite transfer_child | ✅ настройки → Семья → ребёнок → «Передать родителю» (v2026.06.07.14) | — |
| invite_info / accept (посадочная) | — (ссылки строятся на family.html — это её единственная роль) | ✅ посадочная |
| invites (список отправленных) | ✅ настройки → Семья → Приглашения (v2026.06.07.14) | — |
| invite_action (отозвать ✕ / новая ссылка ↻) | ✅ там же (v2026.06.07.14) | — |
| members | ✅ (вся семья: родители + дети) | — |

### 2.2 Остальные эндпоинты
| Файл | Гейт | Примечание |
|---|---|---|
| `data.php`, `state.php`, `stats.php`, `action.php`, `modules.php`, `registry.php` | сессия, иначе fallback «Артём» при `single_user=true` | после флипа `single_user=false` проверить, что без сессии отвечают ошибкой, а не падают |
| `upload.php` | rt_guard | складывает в `uploads/users/{id}/...`, путь в БД |
| `store/*` (enable/install/reorder/uninstall) | `rt_admin_gate` (ТОЛЬКО родительская сессия) | PIN полностью упразднён 2026-06-07 (v2026.06.07.13) |
| `parent.php` (GET) | `rt_require_parent` + `rt_can_read` на ребёнка | данные родительского дашборда: дети, виш-лист ребёнка (фото только primary/семья — provisional не получает пути), события за окно (7–90 дней, лимит 600), очки; ничего не пишет и не логирует |
| `mail_test.php` | `rt_require_admin` (мастер-админ) | переведён с PIN 2026-06-07; вход админом в том же браузере |

### 2.3 Почта (`_mail.php` + `api/mail/`)
- Драйверы log / brevo / phpmail; шаблоны password_reset, invite_co_parent, transfer_child, test × en/ru/lv; реестр `api/mail/registry.php`; лимит писем на адрес.
- С v2026.06.07.3 `accounts.php` шлёт письма ЧЕРЕЗ шаблоны (`rt_mail_send_tpl` + язык пользователя). Легаси `rt_mail_send` больше никем не вызывается (оставлена в `_mail.php` на всякий случай).
- Сейчас `mail_driver=log`: письма НЕ уходят, ссылка пишется в журнал events → приглашения передавать кнопкой «Копировать».

---

## 3. Чего НЕТ в интерфейсе приложения (разрывы)

Отсортировано по важности.

1. ✅ ЗАКРЫТО v2026.06.07.14 — «Забыли пароль?» теперь на lock-экране.
2. ✅ ЗАКРЫТО v2026.06.07.14 — «Передать родителю» в настройках → Семья → шторка ребёнка.
3. ✅ ЗАКРЫТО v2026.06.07.14 — приглашения со статусами + отозвать/новая ссылка в настройках → Семья.
4. **Правила безопасности для ребёнка при первом входе** — в прототипе есть обязательный экран (НЕ фотографируй людей, НЕ загружай свои фото, только простые предметы/картинки из интернета, «если просят другое — скажи родителю»), в приложении его НЕТ. Для детского приложения это пробел №1 по духу проекта.
5. **Принятие приглашения внутри приложения** — `index.html` не понимает `?invite=`/`?reset=`; посадочная — family.html. С v2026.06.07.14 это её ЕДИНСТВЕННАЯ роль (консоль удалена), путь рабочий; перенос посадочных в SPA — опционально, низкий приоритет.
6. **Добровольная смена пароля** — сменить пароль без сброса нельзя нигде (есть только форс-смена 1234 и сброс родителем/по почте).
7. ✅ ЗАКРЫТО v2026.06.07.14 — provisional-дети видны в настройках с пилюлей «гость».
8. **Приватность фото виш-листа НЕ enforced** — правило «фото видят только ребёнок и его ПРЯМОЙ родитель, временный — никогда» (§4.7/§8 плана) задокументировано, но `uploads/` раздаётся веб-сервером как статика без проверки прав (API родительского дашборда provisional'у пути уже не отдаёт). Нужен отдающий PHP-эндпоинт с `rt_can_view_images()` + deny на прямой доступ к папке.
9. Мелочи: у приглашений в приложении не показывается срок жизни (7 дней — теперь в подсказке «Друзья», у co_parent нет); нет экрана «аккаунт заблокирован» (ребёнок получит общую ошибку входа).

## 4. Серверные шаги-условия (не код)
1. В `config.php` на сервере проверить/поправить `app_base_url` → `https://apps.tilley.live/robtop/app/` (пример обновлён). С v2026.06.07.5 короткие ссылки строятся корректно при обоих значениях (хвост `app/` срезается), но единообразие лучше.
2. После проверки обоих входов: `'single_user' => false` (закрыть анонимный API).
3. Когда понадобятся реальные письма: `mail_driver='brevo'` + ключ + DKIM (инструкция в РЕЛИЗ.md), тест через `mail_test.php`.

## 5. Решено и работает (для полноты)
Музыка при чистке зубов; индикатор версии + cache-busting; полный бэкенд аккаунтов (семьи, опекунства primary/provisional, приглашения 4 типов, сессии, логи «детской системы»); lock-экран; семья и друзья в настройках; блокировка с убийством сессий; письма по трёхъязычным шаблонам; деплой вебхуком с раскладкой `/robtop/app/`; документация закрыта `.htaccess`.
