-- 014: «Прогулка» v1.1.0 — отдельное событие «Проблема с поведением» (walk/behavior, вне прогулок,
-- режим «Щенок») + поле «Время прогулки» на экране деталей. Данные — тот же generic-стор
-- module_data, новых таблиц нет; манифест (color/roles/wide) не меняется — только версия модуля.
UPDATE modules SET version='1.1.0', updated_at=NOW() WHERE id='walk';
